@page "/payroll/periods"
@using TTL.HR.Shared.Components.Common

<PageTitle>Quản lý Bảng lương - TTL.HR</PageTitle>

<div class="card card-flush shadow-sm border-0">
    <div class="card-header align-items-center py-5 gap-2 gap-md-5 border-0">
        <div class="card-title">
            <div class="d-flex align-items-center position-relative my-1">
                <i class="ki-outline ki-magnifier fs-3 position-absolute ms-4 text-gray-500"></i>
                <input type="text" class="form-control form-control-solid w-250px ps-12" placeholder="Tìm kiếm bảng lương...">
            </div>
        </div>
        <div class="card-toolbar">
            <button type="button" class="btn btn-primary fw-bold" @onclick="CreateNewPeriod">
                <i class="ki-outline ki-plus fs-2 me-1"></i> Tính lương tháng này
            </button>
        </div>
    </div>
    
    <div class="card-body pt-0">
        <SkeletonLoading IsLoading="_isLoading" Type="SkeletonLoading.SkeletonType.Table" Rows="5">
            <div class="table-responsive">
                <table class="table align-middle table-row-dashed fs-6 gy-5">
                    <thead>
                        <tr class="text-start text-gray-400 fw-bold fs-7 text-uppercase gs-0">
                            <th class="min-w-200px">Kỳ lương</th>
                            <th class="min-w-100px text-center">Tháng/Năm</th>
                            <th class="min-w-100px text-center">Nhân viên</th>
                            <th class="min-w-150px text-end">Tổng chi trả</th>
                            <th class="min-w-120px text-center">Trạng thái</th>
                            <th class="text-end min-w-100px">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody class="fw-semibold text-gray-700">
                        @if (Periods.Any())
                        {
                            @foreach (var period in Periods)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="symbol symbol-40px me-3">
                                                <div class="symbol-label bg-light-success">
                                                    <i class="ki-outline ki-dollar text-success fs-2"></i>
                                                </div>
                                            </div>
                                            <a href="javascript:void(0)" @onclick="() => ViewDetail(period.Month, period.Year)" class="text-gray-800 text-hover-primary fw-bold fs-6">@period.Name</a>
                                        </div>
                                    </td>
                                    <td class="text-center">
                                        <span class="badge badge-light fw-bold fs-7">@period.Month/@period.Year</span>
                                    </td>
                                    <td class="text-center">
                                        <span class="text-gray-600 fw-bold">@period.EmployeeCount nhận viên</span>
                                    </td>
                                    <td class="text-end">
                                        <span class="text-dark fw-bolder fs-6">@period.TotalAmount.ToString("N0") VND</span>
                                    </td>
                                    <td class="text-center">
                                        <div class="badge @(period.Status == "Closed" || period.Status == "Đã chốt" ? "badge-light-success" : "badge-light-warning") fw-bold px-4 py-3">@(period.Status == "Closed" || period.Status == "Đã chốt" ? "Đã chốt" : "Dự thảo")</div>
                                    </td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-icon btn-light-primary btn-active-primary me-2 hover-scale" title="Xem chi tiết" @onclick="() => ViewDetail(period.Month, period.Year)">
                                            <i class="ki-outline ki-eye fs-2"></i>
                                        </button>
                                        <button class="btn btn-sm btn-icon btn-light-danger btn-active-danger hover-scale" title="Xóa">
                                            <i class="ki-outline ki-trash fs-2"></i>
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="6" class="text-center py-20">
                                    <div class="text-muted fs-4">Chưa có dữ liệu bảng lương.</div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </SkeletonLoading>
    </div>
</div>
